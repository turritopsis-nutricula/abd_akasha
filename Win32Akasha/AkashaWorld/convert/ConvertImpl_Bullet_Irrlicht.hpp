#ifndef CONVERT_IMPL_BULLET_IRRLICHT__HPP
#define CONVERT_IMPL_BULLET_IRRLICHT__HPP

#include "ConvertFunction.hpp"

namespace akasha
{
/*
	template<>
	irr::core::vector3df convertType(const btVector3& from)
	{
	return irr::core::vector3df(from.getX(),from.getY(),from.getZ());
	}

	namespace convert
	{
	template<>
	irr::core::vector3df&& convertImpl(const btVector3& from)
	{
	return std::move(irr::core::vector3df(from.getX(),from.getY(),from.getZ()));
	}
	}*/
}

#endif
